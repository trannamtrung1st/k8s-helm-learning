# kubectl context for ./scripts/helm-apply.sh --cluster aks
# Override: KUBECTL_CONTEXT, AKS_RESOURCE_GROUP, AKS_CLUSTER_NAME env vars.
: "${AKS_RESOURCE_GROUP:=workbench}"
: "${AKS_CLUSTER_NAME:=workbench-aks}"
: "${KUBECTL_CONTEXT:=${AKS_CLUSTER_NAME}}"
