# kubectl context for ./scripts/helm-apply.sh --cluster local
# Override: KUBECTL_CONTEXT or KIND_CLUSTER_NAME env vars.
: "${KIND_CLUSTER_NAME:=workbench-0}"
: "${KUBECTL_CONTEXT:=kind-${KIND_CLUSTER_NAME}}"
