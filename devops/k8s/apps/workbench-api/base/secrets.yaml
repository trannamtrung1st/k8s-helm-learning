# Dev / learning defaults — replace with sealed secrets, External Secrets, or CI injection for real clusters.
apiVersion: v1
kind: Secret
metadata:
  name: workbench-api-secrets
type: Opaque
stringData:
  ConnectionStrings__Default: Host=postgres;Port=5432;Database=workbench;Username=workbench;Password=workbench
