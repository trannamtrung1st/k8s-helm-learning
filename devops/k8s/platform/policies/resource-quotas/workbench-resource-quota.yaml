apiVersion: v1
kind: ResourceQuota
metadata:
  name: workbench-resource-quota
  namespace: workbench-apps
spec:
  hard:
    requests.cpu: "2"
    requests.memory: "3Gi"
    limits.cpu: "3"
    limits.memory: "4Gi"
