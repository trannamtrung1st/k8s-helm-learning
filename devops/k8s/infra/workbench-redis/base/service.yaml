apiVersion: v1
kind: Service
metadata:
  name: workbench-redis
spec:
  selector:
    app.kubernetes.io/name: workbench-redis
  ports:
    - name: redis-port
      protocol: TCP
      port: 6379
      targetPort: redis-port
  type: ClusterIP
  clusterIP: None
