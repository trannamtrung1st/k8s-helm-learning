## Workbench job topology. JSON: devops/rabbitmq/definitions.json (Compose + Kubernetes mount the same paths).
definitions.import_backend = local_filesystem
definitions.local.path = /etc/rabbitmq/definitions/workbench.json
definitions.skip_if_unchanged = true
