apiVersion: v1
kind: ConfigMap
metadata:
  name: workbench-cluster-values
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  # Mirrors global.workbenchStorageClasses.pvc.name (Helm). Legacy Kustomize local default.
  pvcStorageClassName: standard
