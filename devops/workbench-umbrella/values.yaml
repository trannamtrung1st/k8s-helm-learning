# Umbrella defaults. Cluster overlays: devops/clusters/<cluster>/global-values.yaml
# Platform values: devops/platform/values/global-values.yaml (required for gateway part-of label).

workbench-public-gateway:
  enabled: true

workbench-http-to-https-redirect:
  enabled: true

workbench-api:
  httpRoute:
    enabled: true
    backendRefs:
      - name: workbench-api
        port: 80
        weight: 50
      - name: workbench-api-v2
        port: 80
        weight: 50

workbench-api-v2:
  name: workbench-api-v2
  instance: workbench-api-v2
  replicaCount: 1
  workbench:
    version: "2.0.0"
  configMap:
    name: workbench-api-v2-config
  secrets:
    name: workbench-api-v2-secrets
  httpRoute:
    enabled: false

workbench-app:
  httpRoute:
    enabled: true
