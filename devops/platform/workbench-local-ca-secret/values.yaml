# Local mkcert CA for cert-manager (opt-in; default off).
#
# Enable and supply tlsCrt / tlsKey via gitignored overlay:
#   devops/clusters/local/local-ca.values.yaml
# Generated by: ./scripts/local-tls-prep.sh

enabled: false
