apiVersion: v2
name: workbench-local-ca-secret
description: |
  TLS Secret for the local mkcert CA (cert-manager CA Issuer input).

  Enable only on local clusters. PEM material is gitignored and generated by
  ./scripts/local-tls-prep.sh into devops/clusters/local/local-ca.values.yaml.
type: application
version: 1.0.0-rc1
appVersion: "1.0.0-rc1"
