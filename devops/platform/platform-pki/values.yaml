# Cluster PKI for cert-manager (root + intermediate CA, ClusterIssuers).
#
# Installed separately by ./scripts/cert-manager-install.sh as Helm release
# workbench-platform-pki (not part of workbench-umbrella).
#
# Requires: ./scripts/cert-manager-install.sh

enabled: true
