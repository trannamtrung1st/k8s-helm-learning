apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ include "workbench.api.namespace" . }}
  labels:
    {{- include "workbench.api.labels.standard" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "workbench.api.labels.selector" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
