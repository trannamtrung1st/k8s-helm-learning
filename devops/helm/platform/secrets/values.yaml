# =============================================================================
# workbench-apps-secrets — values
# =============================================================================
# This chart does not embed connection strings. You MUST merge:
#
#   1) devops/helm/platform/values/global-values.yaml  — structure + secret name
#   2) devops/helm/clusters/<cluster>/global-values.yaml — non-empty
#      global.workbenchRabbitMq.uri and global.workbenchRedis.connectionString
#
# `platform/values/global-values.yaml` leaves those two fields empty on purpose;
# the cluster file supplies real URIs. `values.schema.json` enforces non-empty
# strings after merge so `helm lint` / `helm template` fail if you omit (2).
#
# See: devops/helm/README.md
# =============================================================================

{}
