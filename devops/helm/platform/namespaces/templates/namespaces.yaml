{{- /* Namespace list from .Values.global (works for direct install and umbrella subcharts). */ -}}
{{- $g := .Values.global | default dict -}}
{{- $namespaces := $g.workbenchNamespaces | default dict -}}
{{ $workbenchPartOf := $g.workbenchPartOf }}
{{ range $k := keys $namespaces | sortAlpha }}
{{ with $namespace := index $namespaces $k }}
{{ include "platform.namespaces.namespace" (
    dict
        "name" .name
        "partOf" $workbenchPartOf
        "purpose" .purpose
        "Release" $.Release
        "Chart" $.Chart
) }}
---
{{ end }}
{{ end }}
