# Workbench Redis — reference for Docker Compose and legacy Kustomize (secretGenerator).
# Helm renders workbench.conf from global.workbenchRedis.user/password (cluster global-values.yaml).

appendonly no

user default off
user workbench on >workbench ~* &* +@all
