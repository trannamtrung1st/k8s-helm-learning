apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ include "workbench.lib.namespace.db" . }}
  labels:
    {{- include "workbench.lib.labels.standard.infra" (dict "ctx" . "component" "database") | nindent 4 }}
spec:
  selector:
    {{- include "workbench.lib.labels.selector.nameOnly" . | nindent 4 }}
  ports:
    - name: db-port
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: db-port
  type: ClusterIP
  clusterIP: None
