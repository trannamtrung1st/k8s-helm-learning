# Workbench Redis — shared reference for local Compose (and can be reused for K8s).
# ACL matches apps: StackExchange.Redis user=workbench, password=workbench.

appendonly no

user default off
user workbench on >workbench ~* &* +@all
