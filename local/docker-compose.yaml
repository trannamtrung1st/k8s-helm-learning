# Full stack: infra + workbench-api, workbench-worker, workbench-app
# Run from repo root: docker compose -f local/docker-compose.yaml up --build
#
# App services use image tag 1.0.0-rc1 (bump when releasing). `image` + `build` tags the local build.
# Backend build context: ../src (Workbench.*). Frontend: ../src/workbench-app.

name: workbench

include:
  - path: ./docker-compose.infra.yaml

services:
  workbench-api:
    image: workbench/workbench-api:1.0.0-rc1
    build:
      context: ../src
      dockerfile: Workbench.Api/Dockerfile
    environment:
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Default: >-
        Host=postgres;Port=5432;Database=workbench;Username=workbench;Password=workbench
      RabbitMq__Uri: amqp://workbench:workbench@rabbitmq:5672/
      Redis__ConnectionString: redis:6379
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
    networks:
      - workbench

  workbench-worker:
    image: workbench/workbench-worker:1.0.0-rc1
    build:
      context: ../src
      dockerfile: Workbench.Worker/Dockerfile
    environment:
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Default: >-
        Host=postgres;Port=5432;Database=workbench;Username=workbench;Password=workbench
      RabbitMq__Uri: amqp://workbench:workbench@rabbitmq:5672/
      Redis__ConnectionString: redis:6379
    ports:
      - "8082:8080"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      workbench-api:
        condition: service_started
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 256M
    networks:
      - workbench

  workbench-app:
    image: workbench/workbench-app:1.0.0-rc1
    build:
      context: ../src/workbench-app
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8080
    ports:
      - "8081:80"
    depends_on:
      workbench-api:
        condition: service_started
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
        reservations:
          cpus: "0.10"
          memory: 64M
    networks:
      - workbench
